// How browser will know which is keyword and which is data, string etc. To do this datatypes are used. Data types classify the data into "NUMBER", "STRING", "BOOLEAN" etc.

// In chrome open "Developer tool" in that click on "Console", there i ran these codes.
2+3
5

alert(2+3);
undefined

typeof(123);          // "typeof" is used to get datatype of specific data.
'number'

typeof("Hello");
'string'

typeof(true);
'boolean'
