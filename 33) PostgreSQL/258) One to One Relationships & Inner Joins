Relationship types -
One to One, One to Many and Many to Many.
REFERENCES is keyword used to indicate foriegn key.

CREATE TABLE student (
  id SERIAL PRIMARY KEY,
  first_name TEXT,
  last_name TEXT
);

-- One to One --
CREATE TABLE contact_detail (
  id INTEGER REFERENCES student(id) UNIQUE,  // student id must be unique. There should not be any two id's in contact_detail table for the same record in student table.
  tel TEXT,
  address TEXT
);

// We use -- to add comment while writing sql code

// Use draw.io to make flow diagram
// Primary key id in student table and Foreign key id in contact_detail table are linked together by one to one relationship.

-- Data --
INSERT INTO student (first_name, last_name)
VALUES ('Prasad', 'M S');

INSERT INTO contact_detail (id, tel, address)
VALUES (1, '+123456789', 'abc');

// INNER JOIN - This will display all the data from two tables which has one to one relationship
SELECT *
FROM student
JOIN contact_detail
ON student.id = contact_detail.id;
