document.querySelector("h1");
$("h1");    // short form of above code in jQuery. we can select css selector such as "h1" to modify.

$("h1.title");      // we can select like this to manipulate elements.
$("#header h1");

document.querySelectorAll("button");    // This select all buttons that is defined inside index.html.
$("button");    // This also selects all buttons that is defined inside index.html. If it is single or more, the syntax is same in jQuery.
